[project]
name = "rocket-flight-doctor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.2",
    "numpy>=2.5.3",
    "pandas>=3.0.5",
    "pandera",
    "pint>=0.26.1",
    "pydantic>=2.13.5",
    "pyqtgraph>=0.14.0",
    "pyside6>=6.11.2",
    "scipy>=1.18.1",
    "typer>=0.27.2",
]

[project.scripts]
rocket-flight-doctor = "rocket_flight_doctor:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "mypy>=2.3.1",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    "ruff>=0.16.7",
]
